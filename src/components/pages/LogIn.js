import React from 'react';
import LoginForm from "./LoginForm";

const LogIn = () => {
    return (
        <div className="login">
        <LoginForm/>
        </div>
    );
};

export default LogIn;